# Session Log

One line per session. Newest last.

- 2026-06-09: replaced Excel config with JSON + local HTML editor (`config-editor` CLI) on branch `feat/html-config-editor`; brainstorm → spec → TDD plan → 11 tasks executed sequentially; 708 tests pass.
- 2026-06-09 (later): simplified UX to one `start` command (auto-scaffold + panel + in-panel Save & run); TDD, 718 tests pass, e2e smoke verified.
- 2026-06-09 (later): fixed env (stale install + PATH) with editable install + package `__main__.py`; fixed panel dark-mode contrast and column list layout (light-dark() palette, sorted groups, all/none toggles); verified via live DOM/computed-style inspection.
- 2026-06-09 (later): panel UX round 2 — collapsible column groups, one-click index presets (S&P 500: 503 / Nasdaq 100: 101 / Russell 2000: 1935, bundled from Wikipedia + Vanguard VTWO), viewport-locked no-scroll layout; verified end-to-end in user's Chrome.
- 2026-06-09 (later): run-failure root cause — stale Excel `__main__.py` in repo root + empty API keys; replaced entry script with JSON version, added panel API-keys section writing Config/.env (values never echoed), missing-key now clean ConfigurationError + exit 1. Yahoo extension blocked on Python 3.14 (<3.14 required) — FMP key is the path to live data.
- 2026-06-10: perf diagnosis — download slowness was the unmerged `perf/parallel-fetch-pooled-http` branch (serial urllib on our branch); merged it (one CHANGELOG conflict), 766 tests green, re-benchmarked locally: 3.37s→0.88s (3.8x) at default 8 workers; panel runs get it automatically via main()'s max_concurrent_fetches=8 default.
- 2026-06-10: panel run progress bar (Output file count vs identifiers, one csv per ticker) + mm:ss elapsed timer, 1s polling; verified on a live FMP run (user's key active, 2/2 in 4.6s). Found FMP api key echoed in run logs via URL query strings — added server-side redaction (apikey=***); advised key rotation.
- 2026-06-10: speed round 2 — measured: worker bumps past 8 give nothing (compute-bound); added opt-in max_concurrent_computations, benchmarked 1 vs 4 on light + heavy column sets: zero gain, compute stage GIL-bound (per-row entity assembly: 610k isinstance/480k getattr per 12 tickers). Real S&P 500 run: 0.79s/ticker (fetch ~0.15s). Honest negative result recorded in RESULTS.md; next levers in todo (vectorize entity assembly, process pool, FMP bulk endpoints).
- 2026-06-17: macro catalog comprehensive expansion 17 → 419 verified `e_*` columns / 44 economies. `/deep-research` failed the enumeration (rate-limited WebFetch → all DBnomics breadth scored 0-0, 1 usable series from 3.3M tokens); pivoted to deterministic verifier `scripts/build_macro_catalog.py` hitting provider APIs directly (DBnomics keyless + FRED `fredgraph.csv` via curl). Wide datasets (BIS/IMF-IFS/Eurostat/World Bank) for cross-country consistency; 402 ids live-verified, 51 non-existent dropped. Panel picker auto-updates from the catalog. 806 unit tests green, real end-to-end adapter fetch confirmed. Docs + context updated. Committed on `feat/macro-catalog-expansion`. Then added quarterly support (TDD): DBnomics `_period_to_iso` parses `YYYY-Q[1-4]`; +8 quarterly columns (AU/NZ CPI + EA20/DE/FR/IT/ES/NL real GDP), catalog 419→427, 807 tests green, quarterly e2e fetch verified. PR #3 pushed. Round 2 (max depth): 24-agent direct-API discovery workflow found 24/24 new concept families; integrated as WIDE_CONCEPTS, catalog 427→1292 (44 economies, ~59 indicators), 865 new rows live-verified; added OECD/WB/BIS/IMF-DOT/IMF-WEO sources (all e2e-verified through adapter incl. `:`/`@` paths); panel now groups columns by country (44 collapsible sections); panel restarted; 807 tests + ruff green.
- 2026-06-10: speed round 3 — reimplemented max_concurrent_computations on ProcessPoolExecutor (workers compute + return arrow table; handlers stay in parent, order preserved; custom calc modules re-imported by name). Root-caused BrokenProcessPool on real runs: Windows spawn re-imports __main__ → entry scripts need the if __name__ guard; template fixed + defaults to 4 workers. Real 24-ticker/201-column run: 5.8s→4.6s (-21%), grows with scale. 774 tests green.
- 2026-06-10: panel Excel import — "Load file…" button posts the legacy xlsx to /api/import-excel (parses via ExcelConfigurator extraction classmethods, stamps current format version), fills the panel for review; nothing saved/run until the user clicks Save & run. Verified live in Chrome: fields fill, disk config untouched, run stays idle. 778 tests green.
- 2026-06-10: "didn't worked" after Excel import = user's custom columns (c_*_outlier_adjusted) lack their custom_calculations.py in Config/ → fatal abort that REPORTED SUCCESS (main returned after critical log → exit 0 → panel "Run finished" + 100% bar; critical buried under drain logs). Fixed: main() returns bool, entry template exits 1 on failure, panel surfaces first [CRITICAL] line in status + honest bar. Verified live: status shows the missing-function error on their real 7532-ticker config. User action: copy their custom_calculations.py into Config/ or drop the 3 custom columns.
- 2026-06-10: DuckDB integration — design discussion (DuckDB over SQLite for columnar/analytical fit) → plan → TDD execution on `feat/duckdb-output`; new 'duckdb' output format with single-file DB + (main_identifier, m_date) upsert enabling incremental daily refreshes; 801 tests, ruff, mypy, smoke all green; fetch-side incremental left as scoped follow-up.
- 2026-06-10: 48-min run diagnosis — observer effect: panel polled /api/run 1/s, each poll rglob-stat'ed the whole OneDrive-backed Output dir (~260ms at 7.5k files) while the pipeline wrote into it; mtime rate-curve showed slow middle (0.45s/t) vs fast ends (0.25s/t), end speedup = tab backgrounded → Chrome throttled the timer. Fixed: 5s server-side count cache, 2s polls, full pause when tab hidden (~23ms/poll now). Watched runs ≈ CLI ≈ ~31 min. OneDrive Output location flagged to user (33GB sync churn/run).
- 2026-06-10: configurable output directory — general.output_directory (optional, default Output) in config + panel text field with OneDrive hint; entry-script template pre-reads it before building output handlers; run progress counter follows it; Excel import + old configs default cleanly. Verified live: absolute path round-trips through panel save. 787 tests green. Also measured csv vs parquet on real data (49 vs 38 ms/write, 4.4x smaller) — parquet recommended.
- 2026-06-10 (later): repo migrated to private standalone github.com/alanvaa06/Data-Curator (fork deleted, name reclaimed); merged feat/html-config-editor (contains perf branch) into main; executed high-value fixes: full Excel strip (configurator/CLI/templates/import-excel/openpyxl/docs), configurators raise instead of sys.exit, mypy blocking in CI with legacy baseline, output-handler tests added (11), URLs/badges repointed, repo-wide ruff clean. 794 tests green, mypy green, 6 commits pushed.
- 2026-06-17: launched the panel (synced stale venv: duckdb+networkx were missing); then macro-layer research → proposal. Deep-research (105 agents) + codebase-map (Explore) in parallel: verified Banxico SIE + INEGI as the only adversarially-confirmed Mexico-gate free sources, rejected the aggregator shortcut (unverified/refuted), flagged FRED TOU as an unverified hard-blocker (web verifier got rate-limited, so FRED/DBnomics = unverified not disproven). Confirmed in code that Data Blocks support non-ticker data via grouping_identifier_field=None. Wrote docs/superpowers/specs/2026-06-17-macro-data-layer-proposal.md (gated on Phase-0 verifications). No code changed.
- 2026-06-17 (later): macro-layer capstone on `feat/macro-data-layer`. Closed the blocking mypy gate (2 errors): declared `macro_provider_name: typing.ClassVar[str]` on `MacroDataProviderInterface` (real contract gap — main() reads it; all 4 adapters already set it), and widened `_load_bundled_json` return to `typing.Any` so list/dict catalogs both type-check. Documented the layer (README "Macro / economic data" subsection + FRED non-commercial licensing note, CHANGELOG Unreleased entry, docs/source/data_providers/index.rst macro section). Verification gate all green: 862 tests, ruff clean, mypy 0 errors. Sphinx not installed in venv → docs build not run (RST change is well-formed additive). Open: INEGI GDP/core-CPI series IDs deferred, live-API smoke not run (no tokens), quarter-period support for DBnomics/INEGI pending.
- 2026-06-18: user "macro/FRED didn't work" → root-caused to 0 identifiers (macro broadcasts onto a ticker's market dates via `_infill_data`; none configured → no output, though FRED fetched 200 OK). Chose standalone export with per-series native-cadence files. 5-agent design workflow (wf_2fdd8f1c-83f) mapped touchpoints → TDD build: branch in `main()` (`_export_macro_only`/`_build_macro_series_table`) writes one `date,value` file per `e_*` column reusing existing handlers, fully library-side (no `__main__.py` edits — entry scripts already pass macro providers + handlers). Also fixed the silent 0-identifier trap (now CRITICAL + return False). 6 new tests incl. on-disk CsvOutput integration; 873 tests + ruff + mypy green. Flagged pre-existing workspace `__main__.py` missing the template's try/except as a separate task. NOT yet committed (on main; awaiting user go to branch+commit).
- 2026-06-24: launched panel; user downloaded USDMXN macro-only into a DuckDB that already held tickers → `OutputHandlerError: Binder Error: Referenced column "m_date" not found`. Systematic-debug → root cause: `_build_macro_series_table` emitted `date`, but the DuckDB handler upserts on the existing `curated_data` PK `(main_identifier, m_date)` and `INSERT OR REPLACE ... BY NAME` couldn't bind `m_date` (fresh DB hid it via PK-less wholesale-replace). User picked option (a): unify on canonical `m_date`. TDD: exact-crash repro (pre-existing ticker table) + fresh-DB rows + rerun-dedup + in-memory `export_dataframe` (one-line `date`→`m_date` also fixed the in-memory raise for free); updated the 4 `date`-asserting macro tests + docstrings + CHANGELOG + todo. 877 tests + ruff + mypy green. NOT yet committed (on main; awaiting user go to branch+commit).
- 2026-06-24 (later): ETF browser + custom identifier lists for the config panel. Brainstormed → user chose per-ETF-grouped pills + save-named-lists + all 4 categories → design spec committed. 2-agent research sweep (sectors/factor/broad + thematic) → 31-agent adversarial verify/dedup workflow (wf_c554c346-0fd): 377 verified US ETFs, 4 off-theme dropped, 29 cross-group dups deduped. New `etf_catalog.json` + `load_etf_catalog()` (served as `catalog.etf_groups`); panel `renderEtfBrowser` (collapsible group→subgroup→pill, per-subgroup `all`). Custom lists → `identifier_lists.json` sibling, `read/save/delete_custom_list` + `GET/POST /api/lists` + `POST /api/lists/delete`, ★ pills. Caught + hand-fixed the workflow relocating XLRE out of the 11 Select-Sector SPDRs via dedup priority. TDD; 832 tests + ruff green; verified live in-browser (4 groups/31 subgroups, pill-add/all/save-reload all work, no console errors). Committed on `feat/etf-browser-custom-lists` (spec + feature). Not pushed/PR'd (awaiting user go).
- 2026-06-25: Mexican fondos de inversion on Yahoo. Pushed back on /deep-research (wrong instrument for ticker enumeration, per lessons) -> user agreed. Discovered tickers via Yahoo Lookup API (type=mutualfund) + recursive prefix expansion (2,384 queries -> 3,669 symbols, 3,557 after dropping 112 Morningstar 0P ids), history-verified. 2-agent roster workflow gave operadoras/prefixes + AMIB/CNBV industry stats (29 operadoras, 633 fondos, MXN 4.9bn AUM) + theme taxonomy. Classified operadora (exact, by prefix) + tema (~53%, from clave; Yahoo has no names). Saved research to docs/research/mx-fondos-yahoo/ (CSV/JSON/ANALISIS.md). Integrated as a 2nd panel ticker browser (1,112 base funds, operadora->tema), generalizing ETF browser into renderTickerBrowser. Hit + fixed a Windows cp1252 json.dump encoding bug. 836 tests + ruff green; verified both browsers live. Branch feat/mx-fondos-browser, PR #8. (Earlier same session: ETF browser+custom lists PR #6 merged; provider install-hint fix PR #7 merged.)
- 2026-06-25 (later): MX fund browser flattened to operadora->funds (dropped the tema layer). Brainstormed; corrected the premise (panel already grouped by proveedor/operadora) -> user picked option A (flatten). Render-only: `renderTickerBrowser` gained a `flat` flag via an extracted shared `addRow` builder, `renderFundBrowser` passes true; ETF browser stays 2-level; no catalog edits, tema kept in pill tooltip. Page is read per-request so the live 8753 panel picks it up on refresh. Verified live in preview (8765) via DOM eval: 28 provider rows, BBVA->170 pills direct, provider-level "Add all in BBVA", ETF browser unregressed (4 headers/31 rows), 0 console errors. The `·` (U+00B7) I suspected as mojibake was only my terminal mis-rendering it -> non-bug, untouched. Branch `feat/mx-funds-flat-by-provider`, PR opened.
